import style from './Footer.module.scss';

export const Footer = () => {
    return (
        <footer id={ style.footer }>
            <small>&copy; Lyrad Digital 2021</small>
        </footer>
    );
}